[project]
name = "feedback-management"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.14"
dependencies = [
    "tornado==6.5.8",
]

[dependency-groups]
dev = [
    "ruff==0.16.4",
    "ty==0.0.73",
]
